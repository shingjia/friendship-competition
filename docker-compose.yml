services:
  app:
    build: .
    image: friendship-competition:latest
    container_name: friendship-competition
    ports:
      - "38889:38889"
    volumes:
      - ./data:/app/data
    restart: unless-stopped
